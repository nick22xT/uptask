doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina

            p Coloca tu nueva contraseña
            form#formulario.caja.caja-login(method="POST") 
                .campo 
                    label(for="password") Nueva contraseña: 
                    input(type="password" name="password" placeholder="Nueva contraseña" value= email)
                .campo.enviar 
                    input.boton(type="submit" value="Restablecer Contraseña")