doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            if locals.mensajes
                .alertas 
                     each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            p Agrega tu email para restablecer la contraseña
            form#formulario.caja.caja-login(action="/restablecer" method="POST") 
                .campo 
                    label(for="email") E-mail: 
                    input(type="email" name="email" placeholder="Tu Email" value= email)
                .campo.enviar 
                    input.boton(type="submit" value="Restablecer Password")
                .campo.acciones
                    a(href="/iniciar-sesion") Ya lo recordé. Iniciar Sesión